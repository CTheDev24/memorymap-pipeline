# MemoryMap GUI

Run `npm install`, then `npm run dev`. Vite proxies `/api` to `http://localhost:8000`.

Checks: `npm run typecheck`, `npm test`, and `npm run build`.

The app expects route upload, preview, generate, and job endpoints from the MemoryMap API.
