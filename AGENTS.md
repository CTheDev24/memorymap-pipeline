# Development branch policy

- Keep `dev` functional as the shared development integration branch.
- Make all changes on a dedicated sub-branch of `dev`, never directly on `dev`.
- Check branch and working-tree status before editing; preserve unrelated work.
- Validate relevant behavior before proposing a merge. Do not merge automatically.
